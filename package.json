{
  "name": "syncpad",
  "version": "0.3.0",
  "description": "A private local-first notes app for Windows with live sync over your own network.",
  "main": "src/main.js",
  "scripts": {
    "start": "electron .",
    "serve": "node src/server.js",
    "check": "node --check src/main.js && node --check src/preload.js && node --check src/server.js && node --check src/store.js && node --check src/renderer/app.js",
    "smoke-store": "node scripts/smoke-store.js",
    "smoke-server": "node scripts/smoke-server.js"
  },
  "keywords": [
    "notes",
    "desktop",
    "electron",
    "sync"
  ],
  "author": "koltregaskes",
  "license": "MIT",
  "devDependencies": {
    "electron": "^35.0.0"
  }
}
